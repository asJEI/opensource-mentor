/**
 * Connection-test helpers — re-export from errors to keep Settings imports stable.
 */
export {
  getConnectionErrorMessage,
  getErrorMessage,
  ApiClientError,
  isApiClientError,
} from './errors'
